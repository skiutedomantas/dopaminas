---
import { Image } from "astro:assets";
import hero from "../../src/assets/pagrindine.png";
---

<style>
  h1 {
    color: var(--background-color);
    font-size: 2.1rem;
    line-height: 1.2;
    font-weight: 600;
    margin-bottom: 0.81rem;
  }

  .main-container {
    background-color: var(--primary-color);
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .main-container p {
    color: var(--background-color);
  }

  .left-side {
    padding: 0.94rem;
    max-width: 23.5rem;
  }

  .button-container {
    display: flex;
    gap: 1.25rem;
    margin-top: 2.25rem;
  }

  .button-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 0.625rem;
    background: var(--background-color);
    color: var(--primary-color);
    text-decoration: none;
    font-size: 1.125rem;
    font-weight: 600;
    width: 8.75rem;
    height: 2.8125rem;
    transition: all 0.3s ease;
  }

  .button-link:hover {
    background-color: var(--secondary-color);
    color: var(--background-color);
    cursor: pointer;
  }

  .button-link.secondary {
    background-color: var(--primary-color);
    width: 8.125rem;
    color: var(--background-color);
    border: 2px solid var(--background-color);
    transition: all 0.3s ease;
  }

  .button-link.secondary:hover {
    color: var(--background-color);
    background-color: var(--large-text-color);
    cursor: pointer;
  }

  .hero-image {
    margin-top: 1rem;
    max-width: 400px;
  }

  @media (min-width: 768px) {
    h1 {
      font-size: 2.625rem;
    }

    .button-link {
      width: 10.625rem;
      height: 3.3125rem;
    }

    .button-link.secondary {
      width: 9.375rem;
    }

    .main-container {
      display: flex;
      align-items: center;
      flex-direction: row;
    }

    .left-side {
      max-width: 405px;
      padding: 0;
      margin-left: 1.25rem;
    }

    .left-side p {
      max-width: 25rem;
    }
  }

  @media (min-width: 1024px) {
    h1 {
      font-size: 3rem;
      margin-top: 4.6rem;
    }
    .hero-image {
      max-width: 33rem;
    }

    .button-container {
      margin-bottom: 3.938rem;
    }
    .main-container {
      justify-content: center;
    }

    .left-side {
      max-width: 30.375rem;
    }

    .left-side p {
      margin-top: 2.0625rem;
      margin-bottom: 2.875rem;
    }
  }
</style>

<div class="main-container">
  <div class="left-side">
    <h1>Padedu susigrąžinti judėjimo laisvę</h1>
    <p>
      Gydymas pritaikytas konkrečiai Jums, Jūsų autentiškam kūnui, Jūsų
      sveikatos istorijai
    </p>
    <div class="button-container">
      <a href="/registracija" class="button-link">Registruotis</a>
      <a href="/kontaktai" class="button-link secondary">Susisiek</a>
    </div>
  </div>
  <Image
    class="hero-image"
    src={hero}
    alt="Dominykas Pakarklis"
    loading="lazy"
  />
</div>
